/** @type {import('tailwindcss').Config} */
module.exports = {
  // tainwindcss를 사용할 파일 확장자를 명시해줘야함 *파일명 적을 때 공백이 있으면 안됨
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
